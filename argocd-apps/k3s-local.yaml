# ArgoCD Application for K3s Local Environment (Rancher Desktop)
# This creates an Application that watches the k3s-local environment

apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: kagenti-k3s-local
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default

  # Source: This GitOps repository
  source:
    repoURL: https://github.com/your-org/kagenti-gitops.git  # UPDATE THIS
    targetRevision: main
    path: environments/k3s-local

  # Destination: Local K3s cluster
  destination:
    server: https://kubernetes.default.svc
    namespace: observability

  # Sync policy: Automatic with pruning
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m

  # Health assessment
  ignoreDifferences:
    - group: apps
      kind: Deployment
      jsonPointers:
        - /spec/replicas
