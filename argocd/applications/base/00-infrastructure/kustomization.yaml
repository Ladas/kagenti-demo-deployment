apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Wave 0 - Core Infrastructure
  - gateway-api.yaml
  - cert-manager.yaml
  - tekton.yaml
  - istio-config.yaml
  - keycloak-operator.yaml
  - keycloak-platform-rbac.yaml  # Cross-namespace RBAC for Keycloak secret access

  # Wave 5 - Authentication (depends on keycloak-operator, keycloak-platform-rbac)
  - keycloak.yaml

  # Wave 0 - OAuth2 Proxy (depends on Keycloak being ready)
  - oauth2-proxy.yaml
