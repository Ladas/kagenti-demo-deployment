apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Wave 10 - Kagenti Operators (Kustomize-based)
  # These use kustomize overlays for local development with localhost:5001 images
  - kagenti-operator.yaml
  - platform-operator.yaml
