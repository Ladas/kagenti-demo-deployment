apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Wave 0 - Core Infrastructure (Helm-based)
  - istio-base.yaml
  - istiod.yaml

  # Wave 5 - Infrastructure Services (Helm-based)
  # DISABLED: SPIRE apps failing - temporarily disabled
  # - spire-crds.yaml
  # - spire.yaml
  - container-registry.yaml
  - kiali.yaml
  - opentelemetry-operator.yaml
  # DISABLED 2025-11-18: Korrel8r Operator has same Go runtime panic as Korrel8r service
  # Both quay.io/korrel8r/operator:0.1.7 and quay.io/korrel8r/korrel8r:* crash identically
  # See TODO_TRACING.md Issue 1 for details
  # - korrel8r-operator.yaml

  # Wave 10 - Kagenti Operators
  # NOTE: Both operators deploy identical Tekton pipeline ConfigMaps
  # This causes SharedResourceWarnings but Kubernetes ServerSideApply
  # allows both operators to share ownership of these resources
  # DISABLED: Using kustomize-based overlays instead (see argocd/applications/kustomize/)
  # - kagenti-operator.yaml
  # - platform-operator.yaml
