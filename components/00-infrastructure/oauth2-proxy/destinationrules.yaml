# DestinationRules for OAuth2-Proxy services
# NOTE: OAuth2-Proxy pods HAVE Istio sidecars (oauth2-proxy namespace has istio-injection=enabled)
# Therefore, no special DestinationRules are needed - use default mTLS behavior
