apiVersion: v1
kind: Service
metadata:
  name: research-agent
  namespace: team1
  labels:
    app: research-agent
    ai.openshift.io/agent.class: "a2a"
    ai.openshift.io/agent.name: "research-agent"
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: a2a
  selector:
    app: research-agent
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: research-agent
  namespace: team1
  labels:
    app: research-agent
    ai.openshift.io/agent.class: "a2a"
    ai.openshift.io/agent.name: "research-agent"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: research-agent
  template:
    metadata:
      labels:
        app: research-agent
        ai.openshift.io/agent.class: "a2a"
        ai.openshift.io/agent.name: "research-agent"
    spec:
      serviceAccountName: default
      containers:
      - name: agent
        image: localhost:5001/research-agent:v0.0.1
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080
          name: a2a
          protocol: TCP
        env:
        - name: A2A_HOST
          value: "0.0.0.0"
        - name: A2A_PORT
          value: "8080"
        - name: llm_api_base
          value: "http://ollama.kagenti-system.svc.cluster.local:11434/v1"
        - name: OTEL_EXPORTER_OTLP_ENDPOINT
          value: "http://otel-collector.observability.svc.cluster.local:4317"
        - name: OTEL_SERVICE_NAME
          value: "research-agent"
        resources:
          requests:
            cpu: 100m
            memory: 256Mi
          limits:
            cpu: 1000m
            memory: 1Gi
