"""
Pytest configuration for E2E platform tests.

Note: Shared command-line options (--exclude-app, --only-critical, --app-timeout)
and fixtures (excluded_apps) are defined in tests/conftest.py.
"""

# No additional configuration needed - all shared fixtures are in tests/conftest.py
